// TODO: constructors

namespace CSharpToCixTransform
{
	class Program
    {
        private static void Main(string[] args)
        {
            var solutionPath = args[0];
            var outputPath = args[1];

            // https://stackoverflow.com/a/14534586


            // File.WriteAllText(args[1], outputBuilder.ToString());
        }
    }
}
